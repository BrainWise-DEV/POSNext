# Loaded automatically when running `yarn dev` (mode=development).
# Values prefixed with VITE_ are exposed to client code via import.meta.env.

# Disable the PWA plugin in dev. The production build ignores this file
# and keeps PWA enabled. Flip to "true" if you need to test service-worker
# behaviour against the dev server.
VITE_ENABLE_PWA=false
